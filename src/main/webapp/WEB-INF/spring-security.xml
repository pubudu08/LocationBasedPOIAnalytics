<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:security="http://www.springframework.org/schema/security"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
	http://www.springframework.org/schema/security
	http://www.springframework.org/schema/security/spring-security-3.0.3.xsd">

	<beans:import resource="hibernate-beans.xml" />

	<security:global-method-security
		secured-annotations="enabled" />

	<security:http auto-config="true" use-expressions="true">
<!-- 		<intercept-url pattern="/login*" access="isAuthenticated()" /> -->
<!-- 		<intercept-url pattern="/login*" access="permitAll" /> -->
<!-- 		<intercept-url pattern="/logoutSuccess*" access="isAuthenticated()" /> -->

		<intercept-url pattern="/css/**" access="hasRole('ROLE_ADMIN')" />
		<intercept-url pattern="/js/**" access="hasRole('ROLE_ADMIN')" />
		<intercept-url pattern="/img/**" access="hasRole('ROLE_ADMIN')" />
		<intercept-url pattern="/resources/**" access="hasRole('ROLE_ADMIN')" />
		<intercept-url pattern="/add*" access="hasRole('ROLE_ADMIN')" />
		<intercept-url pattern="/edit*" access="hasRole('ROLE_ADMIN')" />
		<intercept-url pattern="/delete*" access="hasRole('ROLE_ADMIN')" />
<!-- 		<intercept-url pattern="/**" -->
<!-- 			access="hasRole('ROLE_ADMIN','ROLE_USER')" /> -->

		<form-login login-page="/login" default-target-url="/welcome"
			authentication-failure-url="/loginfailed" />
		<logout logout-success-url="/login" logout-url="/j_spring_security_logout" />
		<security:access-denied-handler
			error-page="/accessDenied" />
	</security:http>

	<beans:bean id="webexpressionHandler"
		class="org.springframework.security.web.access.expression.DefaultWebSecurityExpressionHandler" />

	<security:authentication-manager>
		<security:authentication-provider>
			<security:password-encoder hash="md5" />
			<security:jdbc-user-service
				data-source-ref="dataSource"
				users-by-username-query="select EMP_NO, PASSWORD,ACTIVE from employee where employee.EMP_NO = ?"

				authorities-by-username-query="select e.EMP_NO, ur.USERROLE from employee e, userrole ur where ur.USER_ROLE_ID=e.USER_ROLE_ID and e.EMP_NO = ?" />
		</security:authentication-provider>
	</security:authentication-manager>

</beans:beans>